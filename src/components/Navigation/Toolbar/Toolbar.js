import React from "react";

const toolbar = (props) => (
    <header>
        <div>
            MENU
        </div>
        <div>
            LOGO
        </div>
        <nav>
            <ul>
                <li>
                    destination
                </li>
            </ul>
        </nav>
    </header>
);

export default toolbar